apiVersion: v1
kind: Service
metadata:
  name: weather-api-service
  namespace: weatherapi
  labels:
    app: weather-api
spec:
  selector:
    app: weather-api
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
